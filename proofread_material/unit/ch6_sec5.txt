\section{Malleability of Turing Machines}\label{sec:malleableTM}

The \idxnm{malleability} of Turing machines allows algorithms, implemented
on Turing machines possibly, to parse and modify Turing machines
in terms of their G{\"o}del numbers.  For instance, the construction
from the previous section that converts a Turing machine~$M_1$ with
input alphabet $\Sigma = \{0,1\}$ and arbitrary tape alphabet into
an equivalent Turing machine~$M_2$ with tape alphabet~$\Gamma =\{0,1,\#\}$
was entirely constructive, in the sense that there is a (straightforward)
algorithm that maps $\lgn M_1 \rgn $ to~$\lgn M_2 \rgn$. By the Church-Turing
thesis, the mapping $\lgn M_1 \rgn \mapsto\penalty10000 \lgn M_2 \rgn$ and
be computed by a Turing machine.  Similarly, a $k$-tape Turing machine
can be algorithmically converted into an equivalent 1-tape Turing machine.

It is particularly important that the
malleability assumptions used in the proof of Theorem~\ref{thm:Halt0Undecidable}
about the undecidability of~$\THalt_0$ hold for Turing machines.
Specifically, when working with G{\"o}del numbers of Turing machines,
the mapping sending $\lgn M \rgn$ and~$\lgn N \rgn$
to $f^\prime(f(\lgn N \rgn), \lgn M \rgn)$
is computable by a Turing machine.
This construction was crucial to the construction
of a self-referential algorithm~$D_N$ in Section~\ref{sec:SelfReferAlg}.

Let's break this down and consider $f(\lgn M \rgn)$ and
$f^\prime(\lgn M_1\rgn, \lgn M_2\rgn)$ separately.
First consider~$f(\lgn M \rgn)$.  When working with
Turing machines, the function~$f$ has the property that,
for any $w\in\{0,1\}$, $f(w)$~is equal to the G{\"o}del
number of a machine~$M_w$ that ignores its input and outputs the
string~$w$. The machine~$M_w$ uses input alphabet
$\Sigma = \{0,1\}$ and the alphabet
$\Gamma = \{0,1,\#\}$. It starts by using a fixed number of states (two
states in fact) to overwrite its input with~$\#$'s.
It then uses $|w|$ states to write the string~$w$
on the tape. It then uses another constant set of states (one state
can suffice if $w$~is written right-to-left) to position
the tape head on the first symbol of~$w$ and halt.  This is a
total of $|w|+c_f$ many states for some contant~$c_f$. (In fact, $c_f$~can
equal three.)

Second consider $f^\prime(\lgn M_1 \rgn, \lgn M_2\rgn)$. The idea
is that $f^\prime(\lgn M_1 \rgn, \lgn M_2\rgn)$ is the G{\"o}del number
of a Turing machine that first runs the Turing machine~$M_1$ and then
uses the output of~$M_1$ as the input to~$M_2$ and runs~$M_2$. There is
a bit of a complication since, by convention, $M_2$~is expecting a tape
to be completely blank except for its input. But this can be readily fixed.
Either we can modify\label{page:haltAllBlank} $M_1$~so that it erases everything on the tape except
the output string before halting. Or, we can modify $M_2$~to write blanks
into any ``new'' part of the tape before using it. For the former option,
$M_1$~is modified use a new breadcrumbed symbol~$\#^\prime$ to mark the leftmost and rightmost tape cells
visited. As more tape cells are visited, the modified~$M_1$ detects
this and moves the delimiting $\#^\prime$ symbols as needed.
$M_1$~is further modified so that, before halting, it scans left and right
to find the $\#^\prime$~symbols, overwriting all of the visited tape cells
with~$\#$'s except for the tape cells containing
the output. For the latter option, $M_2$~can be modified similarly to use a new
breadcrumbed symbol~$\#^\prime$ to mark the leftmost and rightmost visited tape cells. When
new tape cells need to be visited, the delimiting symbol~$\#^\prime$ is
moved over and the new cells are initialized to hold~$\#$'s.

Detecting and maintaining the delimiting~$\#^\prime$ symbols requires
extra states: it basically replaces each state of $M_1$ or~$M_2$ with
a small (constant) number of new states.
Then eliminating the use of the new symbol~$\#^\prime$, so as to use
the tape alphabet $\Gamma = \{0,1,\#\}$ requires another constant factor blowup
in the number of states: this is based on the construction in the
previous section.  The end result is that the number of states
in the Turing machine with G{\"o}del number $f^\prime(\lgn M_1 \rgn, \lgn M_2\rgn)$
is bounded by $O(n_1+n_2)$ where $M_1$ and~$M_2$ have $n_1$ and~$n_2$ states each.

In conclusion, Turing computable functions, Turing decidable relations, Turing
enumerable relations, etc.\ satisfy the \idxnm{malleability} conditions of
Chapter~\ref{chap:AlgorithmsInformal}.  Thus, all the results of
Chapter~\ref{chap:AlgorithmsInformal} apply to Turing machines. This
includes the undecidability of the halting problems
$\THalt_0$, $\THalt_1$ and $\THalt_\TSelf$ for Turing machines,
the Diagonal Lemma, and Rice's Theorem.
\index{Turing machine|)}


