\section{Truth Tables}\label{sec:truthTables}

\index{truth table|(}
We have already seen a couple of examples of truth tables. This section will
discuss how to write truth tables more compactly, how to form ``reduced'' truth
tables that can require fewer lines, and how reduced truth tables correspond to
decision trees.

The principal purpose of a truth table is to determine
whether or not a formula is satisfiable or tautologically
valid or neither. For a tautology~$A$, a truth table can serve as a proof
that $A$ is valid.

There are straightforward algorithms to build truth tables, and
to verify the correctness of truth tables. The algorithm to build truth
tables works roughly as follows. The input is a formula~$A$.
The algorithm first parses $A$ into subformulas and determines
the distinct variables that appear in~$A$. If there are $k$
distinct variables, the algorithm then writes out a truth table
with $2^k$ lines, one for each possible assignment of truth values
to the $k$~variables. For each of the $2^k$ assignments, the
algorithm uses the definition of truth to compute
the truth values of all of the subformulas
of~$A$, starting with the propositional variables and working
up to the value of the entire formula~$A$. If the value
of~$A$ is discovered to be True~($\True$) for all $2^k$ truth
assignments, the algorithm outputs that $A$ is a tautology.
Otherwise, it outputs that $A$ is not a tautology.
Similarly, if at least one assignment makes the value
of~$A$ true, then $A$~is satisfiable.

This is an example of an ``effective''\index{effective algorithm} algorithm
since the algorithm gives an answer, and the correct answer, for
any input formula~$A$. Unfortunately, it is not a very fast
algorithm, since its run time turns out to
be at least $2^k$ just because it has to create $2^k$ lines
of the truth table. Thus, this is a so-called ``\idxnm{exponential time}'' algorithm, and
as $k$ grows large, it quickly becomes infeasible to run the algorithm
because it is too slow.

For instance, the universe is believed to be approximately 13 billion years
old, which is approximately $4\times 10^{26}$ nanoseconds. Therefore $2^{88}$~is
approximately equal to the age of the universe in nanoseconds. Thus the truth
table for a formula~$A$
with more than about 100 variables would require more lines in its truth table than
the age of the universe measured in nanoseconds. This is clearly not
feasible to form!

\paragraph*{Compact form of truth tables.}
\index{truth table!compact|(}
Figure~\ref{fig:truthpOqpNq} and the left-hand side of
Figure~\ref{fig:p1p2p1} give two examples of truth tables in
traditional notation. In these truth tables, there is a separate column for
each subformula of the formula~$A$ being evaluated.

The right-hand side of Figure~\ref{fig:p1p2p1} gives the
truth table in more compact form. In that table, the formula~$A$
(in this case, $p_1\limplies p_2 \limplies p_1$) is written at the top
of the right column but its subformulas are not rewritten into separate
columns. The truth values of the subformulas of~$A$ are written
below their principal connectives\index{principal connective}. In this
case, the truth values for the subformula $p_2\limplies p_1$
are written below the principal $\limplies$ connective for the subformula.
Likewise, the truth values for the whole formula are written
below the principal connective of the whole formula, namely below the
first $\limplies$~sign in~$A$.

Figure~\ref{fig:pqrEquivPqr} shows
another example of a compact truth table, for the formula
\begin{equation}\label{eq:pqrEquivPqr}
(p \limplies q\limplies r) \liff ( p \land q \limplies r ) .
\end{equation}
This shows that (\ref{eq:pqrEquivPqr})
is a tautology. It follows that
$p\limplies (q \limplies r)$ and
$(p \land q) \limplies r$ are tautologically equivalent
(see Theorem~\ref{thm:pqrEquivPqr}).

\begin{figure}
\begin{center}
\begin{tabular}{ccc|c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c}
$p$ & $q$ & $r$ &
   $[p$ & $\limplies$ & $(q$ & $\limplies$ & $r)]$ &
   $\liff$ & $[ (p$ & $\land$ & $q)$ & $\limplies$ & $r ]$ \\
\hline
$\True$ & $\True$ & $\True$ & & \tikzmarknode{topL}{$\True$} & & $\True$ & & \tikzmarknode{topTrue}{$\True$} & & $\True\tallerMore$ & & \tikzmarknode{topR}{$\True$} \\
$\True$ & $\True$ & $\False$ & & $\False$ & & $\False$ & & $\True$ & & $\True$ & & $\False$ \\
$\True$ & $\False$ & $\True$ & & $\True$ & & $\True$ & & $\True$ & & $\False$ & & $\True$ \\
$\True$ & $\False$ & $\False$ & & $\True$ & & $\True$ & & $\True$ & & $\False$ & & $\True$ \\
$\False$ & $\True$ & $\True$ & & $\True$ & & $\True$ & & $\True$ & & $\False$ & & $\True$ \\
$\False$ & $\True$ & $\False$ & & $\True$ & & $\False$ & & $\True$ & & $\False$ & & $\True$ \\
$\False$ & $\False$ & $\True$ & & $\True$ & & $\True$ & & $\True$ & & $\False$ & & $\True$ \\
$\False$ & $\False$ & $\False$ & & \tikzmarknode{bottomL}{$\True$} & & $\True$ & & \tikzmarknode{bottomTrue}{$\True$} & & $\False$ & & \tikzmarknode{bottomR}{$\True$}
\end{tabular}
\begin{tikzpicture}[remember picture, overlay]
\draw[dotted,line width=0.8pt,rounded corners=1.4ex] ([shift={(-2ex,2.2ex)}]pic cs:topL) rectangle ([shift={(2ex,-0.9ex)}]pic cs:bottomL);
\draw[solid,line width=0.8pt,rounded corners=1.4ex] ([shift={(-2ex,2.2ex)}]pic cs:topTrue) rectangle ([shift={(2ex,-0.9ex)}]pic cs:bottomTrue);
\draw[dotted,line width=0.8pt,rounded corners=1.4ex] ([shift={(-2ex,2.2ex)}]pic cs:topR) rectangle ([shift={(2ex,-0.9ex)}]pic cs:bottomR);
\end{tikzpicture}
\end{center}
\caption{A compact truth table. The solid box shows the values of the whole formula.
The dotted boxes show the values of the two subformulas
$p\limplies q \limplies r$ and $(p\land q)\limplies r$.
The unboxed columns give the truth values of
$q\limplies r$ and $p \land q$.}
\label{fig:pqrEquivPqr}
\end{figure}

\index{truth table!compact|)}

\paragraph*{Reduced truth tables.}\index{truth table!reduced|(}
A big disadvantage of truth tables is that they can be awkwardly big, with
$2^k$ many lines when there are $k$~variables. In many cases, it
is possible to form ``reduced'' truth tables with fewer lines.
Reduced truth tables are based on the fact that sometimes setting
just a few of the variables to true or false is enough to force
the value of the whole formula.  For example, in
Figure~\ref{fig:pqrEquivPqr} the formula will be true whenever $r$
is true. Thus when $r$~is true, the values of $p$ and~$q$ are unimportant.

This insight lets us form the reduced truth table shown in
Figure~\ref{fig:reducedPqrEquivPqr}. The first line of
the reduced truth table considers the case where $\varphi(r) = \True$,
and $\varphi(p)$ and $\varphi(q)$ are not determined.
In that first line, the definition of truth states that
$\varphi( p\land q\limplies r)$ must equal~$\True$ no matter what
the truth value of $p\land q$ is. Likewise, $\varphi(q\limplies r)$
and $\varphi(p\limplies q \limplies r)$ must equal~$\True$.
This lets us fill enough of
the entries on the first line to determine that the entire
formula has truth value~$\True$.
The second line considers the case where $\varphi(r) = \False$
and $\varphi(q)=\False$ and $\varphi(p)$ has not been determined.
The third and fourth lines consider the two cases where
$\varphi(r) = \False$
and $\varphi(q)=\True$ and where $\varphi(p)$ is equal to either
$\True$ or~$\False$.

Figure~\ref{fig:reducedPqrEquivPqr} has half as many rows as
Figure~\ref{fig:pqrEquivPqr},
and still manages to cover all the relevant cases of
how the truth assignment is set.

\begin{figure}
\begin{center}
\begin{tabular}{ccc|c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c}
$p$ & $q$ & $r$ &
   $[p$ & $\limplies$ & $(q$ & $\limplies$ & $r)]$ &
   $\liff$ & $[ (p$ & $\land$ & $q)$ & $\limplies$ & $r ]$ \\
\hline
- & - & $\True$ & & $\True$ & & $\True$ & & \tikzmarknode{topE}{$\True$} & & - & & $\True\tallerMore$ \\
- & $\False$ & $\False$ & & $\True$ & & $\True$ & & $\True$ & & $\False$ & & $\True$ \\
$\True$ & $\True$ & $\False$ & & $\False$ & & $\False$ & & $\True$ & & $\True$ & & $\False$ \\
$\False$ & $\True$ & $\False$ & & $\True$ & & $\False$ & & \tikzmarknode{bottomE}{$\True$} & & $\False$ & & $\True$
\end{tabular}
\end{center}
\begin{tikzpicture}[remember picture, overlay]
\draw[solid,line width=0.8pt,rounded corners=1.4ex] ([shift={(-2ex,2.2ex)}]pic cs:topE) rectangle ([shift={(2ex,-0.9ex)}]pic cs:bottomE);
\end{tikzpicture}
\caption{A reduced truth table. 
Compare to the truth table shown in Figure~\ref{fig:pqrEquivPqr}.}
\label{fig:reducedPqrEquivPqr}
\end{figure}

Unfortunately, even though reduced truth tables sometimes provide
substantial savings in size over full truth tables, they can still require
exponentially many lines. Nonetheless, they can be very useful
when creating small tables by hand.

\begin{example}
Here are two possible reduced truth tables for $p\limplies q \limplies p\land q$:
\begin{center}
\begin{tabular}{cc}
\begin{tabular}{cc|c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c}
$p$ & $q$ &
   $p$ & $\limplies$ & $(q$ & $\limplies$ & $(p$ & $\land$ & $q))$ \\
\hline   
$\False$ & - & & $\True$ & & - & & - \\
$\True$ & $\False$ & & $\True$ & & $\True$ & & - \\
$\True$ & $\True$ & & $\True$ & & $\True$ & & $\True$ \\
\end{tabular}
&
\begin{tabular}{cc|c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c@{\cnspace}c}
$p$ & $q$ &
   $p$ & $\limplies$ & $(q$ & $\limplies$ & $(p$ & $\land$ & $q))$ \\
\hline   
- & $\False$ & & $\True$ & & $\True$ & & - \\
$\False$ & $\True$ & & $\True$ & & - & & - \\
$\True$ & $\True$ & & $\True$ & & $\True$ & & $\True$ \\
\end{tabular}
\end{tabular}
\end{center}
\end{example}

\paragraph*{Reduced truth tables and decision trees.}
\index{decision tree|(}%
Each line in a reduced truth table for a formula~$A$ corresponds to a
``\idxnm{partial truth assignment}''. A \idxem{partial truth assignment}
means an assignment of truth values to some subset of the
propositional variables appearing in~$A$.
A crucial property of reduced truth tables is that the partial
truth assignments cover all possible truth assignments,
in the sense that every possible truth assignment extends at least
one of the partial truth assignments.
This begs the question, however, of how do we know that
the partial truth assignments from a reduced truth table
cover all possible assignments.\footnote{For readers familiar
with the theory of $\Ptime$\index{polynomial time} and 
$\NP$\index{nondeterministic polynomial time}: It is not hard to show that
it is $\NP$-hard to determine if a set of partial truth assignments
covers all possible truth assignments.}

It is thus desirable that reduced truth tables use some pattern
of partial truth assignments
that ensures that they cover every possible truth assignment.
One natural way to do this is to require that the partial
truth assignments in the reduced truth
table correspond to a \emph{binary \idxnm{decision tree}}.  A
binary decision tree means a procedure that queries
the truth values of variables
one at a time: when a variable~$p_i$ is queried, the
procedure receives the truth value $\varphi(p_i)$ of~$p_i$.
Rather than define this formally, we illustrate with an example
based on the reduced truth table of Figure~\ref{fig:reducedPqrEquivPqr}.

A binary decision tree is shown in Figure~\ref{fig:decisionPqrEquivPqr}.
This represents a procedure that traverses a branch in the tree, starting at the root
node, querying the truth value of a variable at each node, and following the
edge labeled with the variable's value to a child node. When a leaf node is reached,
the value of the formula~$A$ is known.

The decision tree in Figure~\ref{fig:decisionPqrEquivPqr}
has its root labeled with~$r$; this is the first variable queried.
There are two edges leaving~$r$, so $r$~has two children.
The first edge is labeled~$\True$ meaning that this edge is
followed when $\varphi(r)=\True$.
The second edge is labeled~$\False$ and that edge is
followed when $\varphi(r)=\False$. One child below~$r$ is a leaf
labeled with ``$\varphi(A)=\True$'': that indicates that once this leaf is reached,
straightforward calculations show that the entire formula~$A$ has
truth value~$\True$ (namely, the calculations in the first
line of the truth table in Figure~\ref{fig:reducedPqrEquivPqr}).

The other child of~$r$ is labeled with $q$, so $q$~is queried next
when $\varphi(r) = \False$.  When $q$ is queried, its value $\varphi(q)$
will be either $\True$ or~$\False$. The process continues similarly until
a leaf is reached. In this example, the leaves are all labeled ``$\varphi(A)=\True$''
since the formula is a tautology.

\begin{figure}
\begin{center}
\begin{tikzpicture}
\node (r) at (0,0) {$r$?};
\node (q) at (1.5,-1.5) {$q$?};
\node (p) at (3,-3.0) {$p$?};
\node (leaf1) at (-1.5,-1.5) {$\varphi(A)=\True$};
\node (leaf2) at (-0.0,-3.0) {$\varphi(A)=\True$};
\node (leaf3) at (1.5,-4.5) {$\varphi(A)=\True$};
\node (leaf4) at (4.5,-4.5) {$\varphi(A)=\True$};
\draw [->] (r) to node[auto,swap,pos=0.33]{$\True$} (leaf1);
\draw [->] (r) to node[auto,pos=0.33]{$\False$} (q);
\draw [->] (q) to node[auto,swap,pos=0.33]{$\False$} (leaf2);
\draw [->] (q) to node[auto,pos=0.33]{$\True$} (p);
\draw [->] (p) to node[auto,swap,pos=0.33]{$\True$} (leaf3);
\draw [->] (p) to node[auto,pos=0.33]{$\False$} (leaf4);
\end{tikzpicture}
\end{center}
\caption{The decision tree corresponding to
the reduced truth table of Figure~\ref{fig:reducedPqrEquivPqr}.
The formula~$A$ is
$(p\limplies q \limplies r) \liff (p \land q \limplies r)$.
The leaves all being labeled $\varphi(A)=\True$ indicates
that $A$~is a tautology.}
\label{fig:decisionPqrEquivPqr}
\end{figure}

We still need to describe what ``straightforward calculations''
can be used at a leaf of the decision tree to determine the
value of~$A$. Some of the variables of~$A$ will have already
been determined to have truth value $\True$ or~$\False$.
We let ``$\True$'' and ``$\False$'' now stand for
a subformula of~$A$ whose value has been determined to be
either true or false. We permit the following two ways for determining
further truth values for subformulas of~$A$ (letting $B$ stand for
any subformula of~$A$):
\begin{enumerateLoose}
\item Any subformula of the form
$\lnot \False$ or $(\True \lor B)$ or $(B \lor \True)$ or $(\True \land \True)$
or $(\False \limplies B)$ or $(B \limplies \True)$
or $(\False \liff \False)$ or $(\True \liff \True)$ can
be determined to have truth value~$\True$.
\item Any subformula of the form
$\lnot \True$ or $(\False \lor \False)$ or $(\False \land B)$ or $(B \land \False)$
or $(\True \limplies \False)$
or $(\False \liff \True)$ or $(\True \liff \False)$ can
be determined to have truth value~$\False$.
\end{enumerateLoose}
If in the end, the truth value of~$A$ has been determined, then
the current node is a valid leaf for the decision tree. If, however,
the truth value of~$A$ does not get determined in this way, then
further variables need to be queried.
\index{truth table|)}\index{truth table!reduced|)}\index{decision tree|)}

